import 'package:flutter/material.dart';
AppSpaces appSpaces=AppSpaces();
class AppSpaces{
  Widget spaceForHeight10=const SizedBox(height: 10,);
  Widget spaceForHeight20=const SizedBox(height: 20,);
  Widget spaceForWidth10=const SizedBox(width: 10,);
  Widget spaceForWidth20=const SizedBox(width: 20,);
}